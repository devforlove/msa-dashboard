bootJar.enabled = false
jar.enabled = true

group = 'com.wook.top.publishercore'
version = '0.0.1'

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
}

test {
    useJUnitPlatform()
}