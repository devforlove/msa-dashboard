server:
  port: 8074

spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092, localhost:9093, localhost:9094
    listener:
      type: batch

  data:
    mongodb:
      uri: mongo:27017