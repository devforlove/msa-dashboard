server:
  port: 8074

spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092, localhost:9093, localhost:9094
    listener:
      type: batch

  data:
    mongodb:
      host: localhost
      port: 27017
      database: playground
      username: inwook.jung
      password: wjddlsdnr12!

feign:
  url:
    post-command: localhost:8072/post