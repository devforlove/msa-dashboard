plugins {
    id 'java-test-fixtures'
}

bootJar.enabled = false
jar.enabled = true

group = 'com.wook.top.kafkaproducer'
version = '0.0.1'

dependencies {
    api 'org.springframework.kafka:spring-kafka'
    testFixturesApi 'org.springframework.kafka:spring-kafka-test'
}